# 最终汇总报告模板

> **⚠️ 严格限定：生成报告时，必须且只能按照本模板的结构和字段进行填充，禁止添加模板中不存在的任何内容。**
>
> **禁止添加的内容包括但不限于**：
> - "已生效调优"、"已验证"等执行状态描述
> - "系统基线"、"最优"等评估性标签
> - "产出数据索引"等非模板定义的章节
> - 任何模板未定义的表格、列表或章节
>
> **正确做法**：严格按照模板结构，将分析得到的数据填入对应字段，不多不少。

---

## 模板正文

```markdown
# 性能调优建议报告

## 报告基本信息

| 项目 | 内容 |
|------|------|
| 报告生成时间 | [YYYY-MM-DD HH:MM:SS] |
| 系统架构 | [x86_64/aarch64] |
| 操作系统版本 | [操作系统名称及版本] |
| 内核版本 | [内核版本号] |
| 主机名 | [主机名] |
| 报告批次 | [YYYYMMDD_HHMMSS] |

---

## 1. 总结

| 瓶颈点 | 类别 | 严重程度 | 影响描述 | 调优手段 | 调优步骤 | 调优脚本 |
|--------|------|----------|----------|----------|----------|----------|
| [瓶颈描述] | [CPU/内存/磁盘/网络/应用/调度] | [严重/高/中/低] | [对系统性能的影响] | [简短调优方向，如：启用PARAL特性] | [精简操作步骤，如：1.启用PARAL 2.设置sched_util_low_pct=100] | [脚本路径，如：./numa-sched-tuning/tuning.sh] |
| [瓶颈描述] | [类别] | [严重程度] | [影响描述] | [简短调优方向] | [精简操作步骤] | [脚本路径] |

---

## 2. 调优建议

### [瓶颈点描述]

**瓶颈证据**：
- [证据1：具体指标数据]
- [证据2：具体指标数据]

**影响分析**：
- [对系统性能的具体影响分析，说明该瓶颈如何影响业务运行]

**调优手段**：
- [简短调优方向描述，与总结表中调优手段一致]

**调优步骤**：
```bash
# 启用PARAL特性
echo PARAL > /sys/kernel/debug/sched/features
# 设置sched_util_low_pct为100
echo 100 > /proc/sys/kernel/sched_util_low_pct
```

**回滚方法**：
```bash
# 禁用PARAL特性
echo NO_PARAL > /sys/kernel/debug/sched/features
# 恢复sched_util_low_pct原始值
echo [原始值] > /proc/sys/kernel/sched_util_low_pct
```

**调优脚本**：`./numa-sched-tuning/tuning.sh apply`

### [瓶颈点描述]

**瓶颈证据**：
- [证据1：具体指标数据]

**影响分析**：
- [对系统性能的具体影响分析]

**调优手段**：
- [简短调优方向描述]

**调优步骤**：
```bash
# [操作描述]
[调优命令]
```

**回滚方法**：
```bash
# [回滚操作描述]
[回滚命令]
```

**调优脚本**：`./[脚本路径]/tuning.sh apply`

---

## 3. 附录

### 参考文档

| 文档名称 | 路径/链接 |
|---------|----------|
| [文档1名称] | [文档路径或链接] |
| [文档2名称] | [文档路径或链接] |
```

---

## 报告输出路径

最终汇总报告保存至：`${WORK_DIR}/tuning/tuning-report.md`

远端场景：报告在 agent 本地生成后 scp 上传到远端该路径（或经远端命令落盘），`${WORK_DIR}` 为远端路径，禁止在 agent 本地创建。详见 `opentunex-remote-execution/references/work_dir_remote_semantics.md`

`${WORK_DIR}` 本身已包含时间戳，本次所有调优产出均在此目录内完成。
