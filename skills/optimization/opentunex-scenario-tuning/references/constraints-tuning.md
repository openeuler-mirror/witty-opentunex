# 调优域强制约束

constraints_version: "v1.0"

## T-01: 禁止直接执行调优命令
不允许在宿主机上或通过远程机器连接执行任何调优命令。

## T-02: 禁止SSH远程执行调优
不允许通过SSH连接远程机器执行调优命令。

- 远端场景（目标为远端服务器）时，`${WORK_DIR}` 为远端路径：调优域对 `${WORK_DIR}` 的文件操作（读融合报告、写契约、写中间态建议、部署调优脚本）须经 `opentunex-remote-execution` 的 ssh 机制在远端执行；调优脚本部署到远端后仍由**用户确认后在远端服务器上执行**（T-01/T-02 不变，agent 不代执行）。详见 `opentunex-remote-execution/references/work_dir_remote_semantics.md`

## T-03: 禁止自动修改系统参数
不允许自动执行修改系统参数的命令。

## T-04: 禁止自动重启服务
不允许自动重启服务或应用。

## T-05: 禁止自动修改配置文件
不允许自动修改应用或系统配置文件。

## T-06: 禁止跳过步骤0
必须先创建调优报告目录，才能执行后续步骤。

## T-07: 中间态输出
调优技能仅生成中间态建议，依据 intermediate-report-template.md 模板，禁止添加模板中不存在的任何内容。

## T-08: 数据前提
融合报告缺失时，必须提醒用户需要先完成瓶颈分析流程，不可在无分析数据的情况下直接调优。

## T-09: 用户确认
调优建议报告中的命令需用户确认后再执行。

## T-10: 禁止内联执行子技能
调度声明中列出的子智能体必须尝试通过子智能体工具启动独立上下文执行，禁止在当前上下文中内联读取子技能 SKILL.md 直接执行。原因：
1. 上下文隔离：子技能在独立上下文中执行，避免主上下文膨胀
2. 并行能力：子智能体可并行执行，内联只能串行
3. 可追踪性：子智能体有独立的输出契约，内联执行无法追踪
4. 职责边界：主智能体只负责调度和汇总，不负责调优逻辑

如果子智能体工具不可用或能力不足（如无法写文件），才允许降级模式，但必须标注 `execution_mode: "degraded"` 并记录降级原因。

**严禁在子智能体工具可用时主动选择降级模式，即使认为内联执行更高效或更方便。** "数据已加载到上下文"不是跳过子智能体的正当理由——这正是 T-10 要防止的可及性偏差。即使数据因用户输入、会话历史、compaction 等原因已在上下文中，仍必须启动子智能体，将数据路径（而非内容）写入输入契约。

**路径不匹配不是豁免条款。** 融合报告路径（`${WORK_DIR}/analysis/` 或用户自定义路径）只是输入契约中 fusion_report 字段的值，路径差异不影响执行步骤。不得因"路径不是标准路径"而判定技能流程不完全适用、跳过任何步骤、或自行简化协议。

仅在子智能体工具确实不可用时（降级模式），才允许逐个读取子技能 SKILL.md 执行，且必须标注 execution_mode: "degraded"。

## 确认方式
子智能体必须在输出契约的 constraints_acknowledged 字段中列出已遵守的约束编号，并在 constraints_version 字段中填写本约束文件版本号。
示例：constraints_acknowledged: ["T-01: 禁止直接执行调优命令", "T-02: 禁止SSH远程执行调优", "T-03: 禁止自动修改系统参数", "T-04: 禁止自动重启服务", "T-05: 禁止自动修改配置文件", "T-06: 禁止跳过步骤0", "T-07: 中间态输出", "T-08: 数据前提", "T-09: 用户确认", "T-10: 禁止内联执行子技能"]
constraints_version: "v1.0"
